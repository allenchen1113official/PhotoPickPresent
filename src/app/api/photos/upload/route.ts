// This route is kept for backwards compatibility.
// LocalUploader now uploads directly to Cloudinary from the browser
// and saves via POST /api/photos, so this route is no longer called.
export {}
